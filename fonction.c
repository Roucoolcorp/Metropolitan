#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void chemin_le_plus_court(station * s_depart, station * s_arrive, reseau * R)
{
	if(strcmp(s_depart->nom, s_arrive->nom) = 0)
	{
		printf("Vous êtes déjà arrivé\n");
		exit(-1);
	}
	int i, y;
	int tab_id[] = malloc(R->nb_station*sizeof(int));

	i=0;
	for(i=0 ; i < R->nb_ligne ; i++)
	{

	}
}

int main()
{
	return 0;
}
